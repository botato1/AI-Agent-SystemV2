# 정확도·화자 개선 후보 (문헌 조사)

2026-08-18. **우리가 실제로 측정한 문제와 연결되는 것만** 담는다.
일반론은 넣지 않는다 — 우리 숫자에 붙지 않는 방법은 시도할 근거가 없다.

우리 오류의 구성(실측, `ded1105f` 기준):

```
cpCER 22.22%  =  전사 7.80%p  +  화자 배정 14.42%p
```

**화자가 전사의 약 두 배다.** 그래서 화자 항목을 앞에 둔다.

---

## 화자 인식

### 1. 임베딩 모델 교체 — 학습 없이, 최우선 ⛔ 카테고리 전체 기각(2026-08-20)

**최종 결론.** ResNet-TDNN(교차 회의 EER 12.47%)과 ECAPA-TDNN(14.38%) 둘 다
cpCER로 재봤는데, 평균이 31.18%/31.19%로 **사실상 동일하게** pyannote(29.35%)보다
나빴다. 서로 다른 모델인데 결과가 이렇게 수렴한다는 건 모델 하나의 문제가
아니라 **"임베딩 모델을 바꾼다"는 접근 자체가 우리 파이프라인과 안 맞는다**는
뜻으로 읽힌다 — 등록·판정 로직이 지금 pyannote 점수 분포에 암묵적으로
맞춰져 있을 가능성. 파인튜닝에 이어 "EER은 대리 지표" 패턴이 세 번째로
확인됐다. 자세한 수치는 `NEXT.md` 2번.

**이 카테고리는 당분간 재시도하지 않는다** — WavLM(EER 25.82%, 넷 중 최악)까지
포함해 4개 후보 중 상위 둘이 이미 같은 방향으로 실패했으므로, 남은 WavLM을
마저 재는 것은 시간 대비 정보 가치가 낮다.

**2026-08-18 실측** — `probe_embedding_models.py`, 우리 회의 4건, **교차 회의 EER**
(같은 사람 쌍을 서로 다른 회의에서 뽑는다 — 등록과 판정이 다른 회의인 실제 조건):

| 모델 | 교차회의 EER | 김나연↔이승주 |
|---|---:|---:|
| **ResNet-TDNN** (speechbrain) | **12.47%** | 0.632 |
| ECAPA-TDNN (speechbrain) | 14.38% | 0.617 |
| wespeaker-resnet34-LM (현재) | 17.32% | 0.606 |
| WavLM base+ SV | 25.82% | 0.968 |

**ResNet-TDNN이 EER을 17.32% → 12.47%로 낮춘다**(상대 28%). 학습이 전혀 필요 없다.

> **교차 회의 EER 17.32%가 우리 실제 과제의 난이도다.** AI-Hub에서 같은 회의 안으로
> 잰 9.68%의 두 배에 가깝다. 앞으로 화자 모델을 바꿀 때는 이 값으로 비교한다.

⚠️ **모델이 다르면 코사인 값을 직접 비교하면 안 된다.** 척도가 다르다.
   ResNet-TDNN의 분포는 같은 사람 평균 0.469 / 다른 사람 0.206인데, 그 척도에서
   김나연↔이승주 0.632는 **같은 사람 평균보다 높다.** 즉 이 모델에서도 그 짝은 여전히 심각하다.
   (등록 방식을 비교할 때처럼 같은 모델 안에서는 코사인 직접 비교가 유효하다.)

**그래서 알게 된 것: 김나연↔이승주는 어떤 모델을 써도 남는다.** 모델 교체로 풀 문제가
아니라 2번(묶음 판정)이나 `NEXT.md` 7번(헷갈리는 짝 처리)의 영역이다.

~~**채택 보류 — cpCER 확인 전까지.**~~ 2026-08-20에 실측 완료, 위 최종 결론대로 기각.

> speechbrain은 `--no-deps`로 설치했다(`speechbrain`, `hyperpyyaml`, `sentencepiece`,
> `ruamel.yaml<0.18`). ruamel은 0.19에서 API가 바뀌어 hyperpyyaml과 안 맞아 0.17로 고정했다.

### 1-2. (원래 계획) 임베딩 모델 교체 근거

**왜 지금 1순위인가.** 오늘 파인튜닝이 실패한 자리를 **학습 없이** 대신할 수 있다.
우리가 쓰는 `wespeaker-voxceleb-resnet34-LM`은 VoxCeleb 기준 EER 1% 안팎인데,
그보다 확실히 나은 기성품이 나와 있다.

- **WavLM-ECAPA-TDNN** — WavLM-Large의 프레임 표현을 ECAPA-TDNN에 넣어 192차원 임베딩.
  ESPnet-SPK 보고 기준 **Vox1-O EER 0.39%** (고정 WavLM 0.60%, 멜 스펙 0.85%)
- **ECAPA2** — 하이브리드 구조로 견고한 임베딩을 노린 후속 연구

**우리 상황과의 연결.** 오늘 잰 한국어 회의 EER이 **9.68%**다. 영어에서 1%인 모델이
10배로 나빠지는 조건이므로, **더 강한 백본이 그 낙폭을 줄여줄 여지가 크다.**
그리고 우리 실패 지점(김나연↔이승주 0.606)이 개선되는지는 `enroll_multi_meeting.py --dry-run`
1분이면 확인된다.

**드는 일.** `SPEAKER_EMBEDDING_MODEL`을 바꾸고 프로필을 다시 만들면 끝.
측정 도구(`eval_speaker_eer.py`)는 이미 있다.

> ⚠️ 임베딩 모델을 바꾸면 **기존 프로필이 전부 무효**다(공간이 달라짐).
> `enroll_multi_meeting.py`로 다시 만들면 되므로 사람이 재녹음할 필요는 없다.

### 2. 묶음 단위 판정 (cluster-then-label)

**표준 설계다.** 임베딩을 병합 군집(AHC)으로 묶고, **묶음 대표값**으로 화자를 식별하는
방식이 최근 시스템의 기본형이다(Shankar 2025 — AHC + ECAPA-TDNN + Whisper).

**우리 근거.** 회의 내 프로필 판정 정확도가 **86~100%**로 등록 프로필(82~95%)보다
세 회의 모두에서 높다. 회의 안의 구조가 더 믿을 만하다는 뜻이고, 지금은 그걸 안 쓰고 있다.

`NEXT.md` 3번과 같은 항목.

### 3. 명단 밖 거부 — 화자별 적응 문턱 (= `NEXT.md` 4번, 같은 항목)

⚠️ **지금 시도하면 여섯 번째 실패가 될 위험이 크다.** `EXPERIMENTS.md`의 다섯 번
기각(문턱 0.30/0.25/0.15, z정규화, 회의 내 적응, 미상 경계)이 전부 같은 이유로
죽었다 — "판정이 끝난 뒤를 만졌고, 곡선 위를 오갔을 뿐 곡선을 옮기지 못했다."
화자별 문턱도 **점수가 나온 뒤 그 점수를 어떻게 자를지**를 만지는 것이라 같은
범주다. **1번(임베딩 교체)이나 2번(묶음 판정)처럼 판정에 들어가는 신호 자체를
바꾸는 작업이 먼저 채택된 뒤에 재시도할 것** — 신호가 바뀌면 EER 지점(0.340)
자체가 이동하므로 이 결론이 무효화될 수 있다. 순서를 지키지 않고 지금 화자별
문턱부터 재면 또 같은 곡선 위를 도는 것일 가능성이 높다(2026-08-20 판단, 근거는
위 인용 그대로).

**우리 문제.** 다중 회의 등록으로 명단 밖 오수락이 **1/11 → 3/11**로 늘었다.
지금은 전원 공통 하한 **0.35** 하나로 신원 확인과 외부인 거부를 겸한다.

**문헌의 진단이 우리 측정과 같다.** 문턱 하나로는 두 오류율의 트레이드오프를 벗어날 수
없다는 것이 공통된 결론이다. 우리는 그걸 "하한 0.35가 이미 EER 지점(0.340)"으로 확인했다.

**대안.**
- **화자별 문턱** — 사람마다 점수 분포가 다르므로(우리 실측 0.29~0.53) 문턱도 따로.
  상용 시스템의 표준 방식이다
- **ReTA(2025)** — 재구성 오차로 미지 화자의 성질을 흉내 내고, 커널 밀도 추정으로
  화자마다 최적 거부 문턱을 계산
- **Speaker RPL(2025)** — 미지 표본 학습을 넣어 단발 개방집합 화자 식별을 개선
- **VoxWatch** — 개방집합 화자 인식 벤치마크. 평가 설계를 참고할 만하다

### 4. 점수 정규화 (AS-norm) — 기대치는 낮게

우리는 z정규화를 시도해 **기각**했다. 코호트 기반 AS-norm은 방식이 다르지만,
문헌도 "정규화만으로 수용률과 거부율을 동시에 올리는 데는 한계가 있다"고 본다.
1~3번을 먼저 하고 남으면 시도한다.

### 5. 파인튜닝 재시도 — 조건을 바꿔서만

오늘 도메인 과적합으로 실패했다(우리 화자가 +0.25씩 가까워짐).
같은 설정으로 반복하지 말 것. 손볼 곳: 백본 학습률 하향, 앞쪽 층 동결,
우리 녹음 혼합. **교차 언어 적응(reprogramming)** 접근도 있다 —
화자 검증 시스템을 다른 언어로 옮기는 문제를 정면으로 다룬 연구다.

---

## 전사

### 6. LLM 생성형 오류 교정 (GER)

ASR이 낸 N-best 후보를 LLM에 주고 **새 전사를 생성**시키는 방식.
후보 중 고르는 게 아니라 다시 쓰는 것이라, 후보 전체보다 나은 결과가 나올 수 있다.
영어·중국어에서 **CER/WER 최대 1.5%p** 개선 보고.

**우리 상황.**
- `probe_llm_correction.py`가 이미 있다
- 다만 우리는 **beam 1**이라 N-best가 없다 — beam을 키우거나 다른 입력을 줘야 한다
- **LLM은 승주 담당**이라 협의 필요. 요약·모순 감지와 같은 모델을 쓸지도 정해야 한다

### 7. 희귀어·고유명사 교정 (발음 문맥 활용)

LLM에게 **발음형을 함께 예측**하게 해서 텍스트와 음운 정보를 같이 쓰는 방식
(합성 데이터 + 발음 문맥, 2025).

**우리 실측 오인식이 정확히 이 부류다** — "승주"→"승준", "WAV"→"WEV",
"번복"→"반복", "재논의"→"재무의". 지금은 정적 용어 목록 9개로 막고 있는데,
그건 **미리 적어둔 단어만** 커버한다.

### 8. 음성 향상 — 도입한다면 CER로 검증

소음 회의에서 CER 32%가 나왔으므로 후보이긴 하다. 다만 잡음을 지우며 음성도
뭉개져 인식률이 오히려 나빠지는 경우가 흔하다. 작년 팀 문서도 SDR·ESTOI 같은
**음질 지표**로 평가했지 인식률로 평가하지 않았다.
도입한다면 반드시 **CER로 전후 비교**할 것.

**2026-08-26 착수 시도 — 설치 단계에서 보류.** 82서버(GPU, ARM64/aarch64,
stt_venv 공유 환경)에 실제로 두 라이브러리를 시도했다:

- **DeepFilterNet**: 핵심(DeepFilterLib)이 Rust로 짜여 있어 Cargo 컴파일이
  필요했고(설치함), 빌드는 성공했지만 **numpy를 2.5.x → 1.26.4로 강제
  다운그레이드**해서 `pyannote-core`/`pyannote-metrics`/`scipy`(numpy≥2.0
  요구)와 정면 충돌했다. **서버 재시작 시 화자분리 자체가 깨질 뻔한
  위험한 상황**이었다 — `pip uninstall`로 즉시 되돌리고 서버 정상 재기동
  확인함. ⚠️ 이 공유 venv에 직접 설치하면 안 된다. 격리된 별도
  프로세스/venv로만 시도할 것.
- **pyrnnoise**: dry-run에선 numpy 충돌 없이 깨끗했으나(사전 컴파일된
  aarch64 wheel 있음), 실제 임포트 시 `pyrnnoise/__init__.py`가 무조건
  끌어오는 의존 패키지 `audiolab`이 이미 설치된 `av`(PyAV) 18.0.0과
  런타임 비호환(`ModuleNotFoundError: av.option`)이었다. `audiolab`
  최신화도 무의미(pip 메타데이터상 이미 "만족"으로 나오는데 실제론 깨짐 —
  `audiolab` 자체의 버그로 보임). `pyrnnoise`가 `audiolab`을 우회할
  경로를 코드 구조상 제공하지 않아 여기서 막힘.

**다음에 시도한다면**: ① `av` 버전을 낮춰서 `audiolab` 호환 확인 (단,
`av`를 우리 코드는 안 쓰지만 다른 의존성이 최신 버전을 요구할 수 있어
dry-run 필수), 또는 ② `audiolab`/파일 I/O 의존성이 없는 다른 RNNoise
바인딩 탐색, 또는 ③ 애초에 격리된 venv/서브프로세스로 갈 것(이러면 이
버전 충돌들이 우리 서버에 영향을 못 준다 — 지연 비용과 맞바꾸는 선택).
**공유 venv에 새 오디오 처리 라이브러리를 설치할 땐 반드시 `pip install
--dry-run`으로 numpy 재설치 여부부터 확인**할 것 — 오늘 DeepFilterNet이
그걸 건너뛰었으면 실제 서버가 깨질 뻔했다.

**⛔ 2026-09-07 — 고역통과 필터(설치 없는 저비용 시도) 기각.** 본격적인
라이브러리 재도전 전에, 새 의존성 없이 scipy 고역통과 필터(150/200/250Hz)
만으로 8b5f84b7의 저역 소음(100~180Hz, `analyze_noise.py`로 확인 — NEXT.md
7번 참고)이 줄어드는지 `try_highpass_denoise.py`로 먼저 확인했다.

**전부 악화됐다.** 원본 cpCER 90.76%/화자대가 58.17% 대비, hpf200에서
cpCER 97.29%·화자대가 60.65%로 더 나빠졌고, hpf150/250은 화자가 5명→
11~12명으로 쪼개져 `meeteval`이 채점 자체를 거부했다. **원인 추정**: 150~
250Hz는 성인 화자의 기본 주파수(F0) 대역과 겹친다 — 소음만 깎으려다 화자
임베딩이 목소리를 구분하는 데 쓰는 신호까지 같이 잘라내서 화자분리가
붕괴한 것으로 보인다. **단순 주파수 필터로는 이 문제를 못 푼다** — 소음과
음성 신호가 저역에서 겹쳐 있어 주파수만으로 분리가 안 된다. 다음은 격리
venv에서 DeepFilterNet(적응형·모델 기반) 재시도.

**⛔ 2026-09-08 — DeepFilterNet도 같은 이유로 기각.** 격리 venv(`setup_dfn_venv.sh`,
공유 stt_venv 무손상 확인)에서 DeepFilterNet3로 8b5f84b7을 향상시켜 재시도했다.
**결과는 고역통과 필터와 완전히 같은 실패 양상** — 화자가 5명→12명으로 쪼개져
`meeteval`이 채점 자체를 거부(`RuntimeError: Found a total of 12 speakers`).
cpCER 수치조차 못 뽑았다.

**결론: 개별 필터/모델의 문제가 아니라 구조적 문제다.** 두 방법(단순 주파수
필터 vs 신경망 기반 적응형 향상)이 정확히 같은 실패로 끝났다는 게 근거다 —
`refine()`은 audio.wav가 조금이라도 바뀌면 화자분리를 **처음부터 다시 돌리는데**,
pyannote 화자 임베딩이 원본과의 미세한 파형 차이에도 극도로 민감해서 어떤
잡음 제거를 걸어도 화자분리가 붕괴하는 것으로 보인다.

**막힌 카테고리로 본다: "회의 전체 오디오에 잡음 제거를 걸고 그걸로 화자분리까지
다시 돌리는" 접근 자체가 안 된다.** 남은 길은 화자분리는 원본으로 하고 전사만
잡음 제거된 오디오로 하는 하이브리드뿐인데, 이건 `_refine_group()`처럼 화자별
구간을 먼저 원본으로 떼어낸 뒤 그 구간 오디오만 향상시켜 전사하는 구조라
지금 있는 시험 스크립트로는 안 되고 재분석 파이프라인 자체를 건드려야
한다 — 다음에 이 방향을 시도한다면 별도 설계가 필요하다.

---

## 측정 기반

### 9. 회의 녹음 확충

지금 모의 회의 3건이 전부다. `8b5f84b7` 하나가 평균을 좌우하고,
n=3이라 신뢰구간을 말하는 것 자체가 성립하지 않는다.
**겹침(동시 발화) 구간을 일부러 넣어** 녹음하면 아직 한 번도 못 잰 항목도 함께 검증된다.

---

## 권장 순서

1. **임베딩 모델 교체**(1번) — 가장 싸고 값이 크다. 학습 없이 하루면 확인 가능
2. **묶음 단위 판정**(2번) — 남은 문제 셋을 동시에 겨냥
3. **화자별 문턱**(3번) — 1·2번 뒤에 남는 오수락을 겨냥
4. 그다음에 전사 쪽(6·7번). 화자가 오류의 2/3이므로 순서가 뒤다

⚠️ 어느 것을 하든 **EER만 보고 채택하지 말 것.** 오늘 파인튜닝이 그 함정이었다 —
EER 9.68% → 7.63%인데 우리 회의에서는 나빠졌다. 반드시 cpCER로 확인한다.

---

## 출처

- [ESPnet-SPK: full pipeline speaker embedding toolkit](https://arxiv.org/pdf/2401.17230) — WavLM-ECAPA-TDNN Vox1-O EER 0.39%
- [ECAPA2: A Hybrid Neural Network Architecture](https://arxiv.org/pdf/2401.08342)
- [An Investigation of Reprogramming for Cross-Language Adaptation in Speaker Verification](https://arxiv.org/pdf/2411.11353)
- [Speaker Diarization: A Review of Objectives and Methods](https://www.mdpi.com/2076-3417/15/4/2002)
- [Reject Threshold Adaptation for Open-Set Model Attribution](https://arxiv.org/pdf/2412.01425) — ReTA
- [VoxWatch: An open-set speaker recognition benchmark on VoxCeleb](https://arxiv.org/html/2307.00169)
- [LLM-based Generative Error Correction for Rare Words with Synthetic Data and Phonetic Context](https://arxiv.org/pdf/2505.17410)
- [Listening, Imagining & Refining: A Heuristic Optimized ASR Correction Framework with LLMs](https://arxiv.org/html/2509.15095v1)
- [When Large Language Models Meet Speech: A Survey on Integration Approaches](https://arxiv.org/pdf/2502.19548)

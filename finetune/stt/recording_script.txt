STT 파인튜닝용 낭독 스크립트 (40문장)

녹음 방법:
- 한 문장당 하나의 파일로 녹음
- 파일명: {이름}_{번호 3자리}.wav  (예: 이준오_001.wav)
- 또박또박 낭독하지 말고, 평소 회의에서 말하듯 자연스러운 속도로

001. 이번 스프린트에서 레이턴시 개선을 위해 임베딩 캐싱을 리팩토링하기로 했습니다.
002. 백엔드 API 엔드포인트에 인증 미들웨어를 추가하고 배포까지 완료했습니다.
003. 도커 컨테이너 빌드가 실패해서 롤백하고 로그를 확인했습니다.
004. 화자분리 모델의 임베딩 유사도 임계값을 조정해서 정확도를 올렸습니다.
005. 웹소켓 연결이 끊기면 재연결 로직이 세션을 복구합니다.
006. 파인튜닝에 LoRA를 적용하면 GPU 메모리를 크게 아낄 수 있습니다.
007. RAG 검색 결과가 부정확해서 벡터 인덱싱을 다시 했습니다.
008. LLM이 환각을 일으켜서 프롬프트를 수정했습니다.
009. 풀리퀘스트 리뷰 끝나면 develop 브랜치에 머지해 주세요.
010. 프론트엔드에서 무한 스크롤 구현할 때 디바운싱 처리했나요?
011. 데이터베이스 마이그레이션 스크립트를 커밋했으니 풀 받아 주세요.
012. 모순 감지 엔진이 과거 결정과 충돌하는 발언을 실시간으로 잡아냅니다.
013. VAD로 침묵 구간을 잘라내니까 환각이 확실히 줄었습니다.
014. 쿠버네티스 클러스터에서 파드가 계속 재시작되는 이슈가 있습니다.
015. 스프린트 백로그에 옵저버빌리티 대시보드 작업을 추가했습니다.
016. 비동기 처리 중에 레이스 컨디션이 발생해서 락을 걸었습니다.
017. 토큰 사용량이 급증해서 캐싱 전략을 다시 검토해야 합니다.
018. 청크 단위로 오디오를 잘라서 실시간 스트리밍으로 처리합니다.
019. 깃허브 액션으로 CI 파이프라인을 구성하고 테스트를 자동화했습니다.
020. 스키마 변경 사항은 마이그레이션 없이 반영되지 않습니다.
021. 디버깅하다 보니 널 포인터 예외가 큐 처리 로직에서 발생했습니다.
022. 서버 크래시 원인은 메모리 누수였고 핫픽스를 배포했습니다.
023. 웹훅으로 슬랙 알림을 연동해서 배포 상태를 공유합니다.
024. 트랜스포머 모델을 양자화하면 추론 속도가 빨라집니다.
025. 파이썬 가상환경에 의존성을 설치하고 요구사항 파일을 갱신했습니다.
026. 프록시 설정 때문에 웹소켓 핸드셰이크가 실패했습니다.
027. 코드 리뷰에서 중복 로직을 함수로 추출하라는 피드백을 받았습니다.
028. 로드밸런서 뒤에 헬스체크 엔드포인트를 열어 두었습니다.
029. 세션 아이디를 검증하지 않으면 보안 취약점이 됩니다.
030. 임베딩 벡터를 코사인 유사도로 비교해서 화자를 매칭합니다.
031. 오디오 샘플레이트를 16킬로헤르츠로 다운샘플링해서 전송합니다.
032. 빔서치 크기를 늘리면 정확도는 오르지만 지연이 커집니다.
033. 커밋 메시지 컨벤션을 정해서 히스토리를 깔끔하게 유지합시다.
034. 스테이징 환경에서 검증한 뒤 프로덕션에 배포합니다.
035. 쿼리 성능이 느려서 인덱스를 추가하고 실행 계획을 확인했습니다.
036. 회의록 요약은 LLM이 초안을 만들고 사람이 확정합니다.
037. 사전 등록된 화자 프로필로 닫힌 집합 매칭을 수행합니다.
038. API 응답 포맷을 JSON 스키마로 문서화했습니다.
039. GPU 메모리가 부족해서 배치 크기를 줄였습니다.
040. 다음 스프린트 킥오프는 월요일 오전에 진행하겠습니다.

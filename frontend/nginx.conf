server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA 라우팅 - 없는 경로는 전부 index.html로 돌려서 클라이언트 라우터가 처리하게 함
    location / {
        try_files $uri $uri/ /index.html;
    }
}

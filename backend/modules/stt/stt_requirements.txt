# ==========================================
# Vigo Project V5.0 - STT(faster-whisper) & Diarization
# RTX 5090 GPU 서버 기준
# ==========================================

# 1. Web Server & API
fastapi==0.138.2
uvicorn==0.49.0
python-multipart>=0.0.9
pydantic==2.13.4

# 2. Environment & Security
python-dotenv==1.2.2

# 3. STT 엔진 (faster-whisper, CTranslate2 기반)
faster-whisper==1.2.1
ctranslate2>=4.3.0

# 4. Speaker Diarization
torch>=2.2.0
torchaudio>=2.2.0
pyannote.audio==4.0.5
soundfile==0.14.0
huggingface_hub==1.21.0

# ==========================================
# 사전 준비 사항 (서버 켜기 전)
# ==========================================
# 1. NVIDIA 드라이버 + CUDA 13.x 설치 확인 (nvidia-smi로 확인)
# 2. .env 파일에 HF_TOKEN=hf_xxxxx 설정 (pyannote 모델 접근용)
# 3. ffmpeg 시스템 설치 확인 (apt install ffmpeg 또는 brew install ffmpeg)
# 4. 첫 실행 시 large-v3 모델(약 3GB)이 자동 다운로드됨 (~/.cache/huggingface)
#
# 서버 실행:
# uvicorn voice_server.main:app --host 0.0.0.0 --port 8001
#
# 주의: --reload 옵션은 GPU 모델 재로딩 문제로 사용하지 않습니다.
# 코드 수정 후에는 수동으로 서버를 재시작하세요.
# 실시간 전사 인식 힌트(initial_prompt)에 넣을 용어 목록 — 런타임 전용.
#
# finetune/stt/terms.txt(66개)는 "평가용 용어 재현율 측정" 목적이라 넓게 담지만,
# 이 파일은 디코딩 힌트로 실제 모델에 주입되므로 의도적으로 짧게 유지한다.
# 목록이 길수록 말하지 않은 용어를 지어내는 부작용이 커지고, Whisper 프롬프트는
# 224토큰 제한도 있기 때문.
#
# 선정 기준 (임의 선택이 아님):
#  ① 실측으로 오인식이 확인된 것 — "임베딩"→"인벨딩", "WAV"→"WEV", "STT"→"std"
#     (2026-07-27 지수 제보 2인 회의 테스트)
#  ② 영문 약어 — 한국어 문맥에서 Whisper가 특히 취약한 부류이고, 위 오인식 사례도
#     대부분 여기 해당
#  ③ 우리 회의에서 실제 빈출하는 도메인 용어
#
# 늘리기 전에 반드시 AI Hub held-out으로 회귀를 확인할 것 — 일반 발화가 나빠지면
# 힌트가 오히려 손해다.
#
# ⚠️ 숫자(포트 번호 등)는 의도적으로 넣지 않음 — 실측으로 효과 없음이 확인됨.
# "포트: 8000, 8001, 8002, 8003, 8082, 8086, 8100."을 힌트에 추가해 실제 회의 오디오의
# 포트 번호 발화 2구간을 재전사해본 결과(2026-07-27):
#   - "8003 서버" 구간: 힌트 없을 때 "팔천 삼 서버" → 힌트 넣으니 "8000 사항 서버"로 악화
#   - "8100 포트" 구간: 힌트 없을 때 "8000일 포트" → 힌트 넣으니 "80001 포트", 여전히 오답
# 힌트는 "어떤 어휘가 나올 수 있는지"를 알려줄 뿐, 못 알아들은 숫자 값을 대신 채워주지
# 않는다. 오히려 목록에 있는 숫자로 잘못 끌려가는 부작용이 관측됐다.
# 숫자 정확도는 회의록에서 사람이 확인·수정하는 영역으로 남긴다
# (PATCH /api/meetings/{meeting_id}/segments/{index}).

# ① 오인식 확인됨
임베딩
WAV
STT

# ② 영문 약어
API
JSON
GPU
LLM
RAG
LoRA
VAD
CI

# ③ 빈출 도메인 용어
레이턴시
화자분리
웹소켓
파인튜닝
청크
임계값
파이프라인
엔드포인트
롤백
마이그레이션
브랜치
머지

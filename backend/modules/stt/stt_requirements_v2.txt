fastapi==0.138.2
uvicorn[standard]==0.49.0
python-multipart>=0.0.9
pydantic==2.13.4
python-dotenv==1.2.2
faster-whisper==1.2.1
pyannote.audio==4.0.5
soundfile==0.14.0
huggingface_hub==1.21.0

# aarch64+CUDA 서버(ctranslate2가 GPU wheel 미배포)용 대체 엔진
# faster-whisper(ctranslate2)가 정상 동작하는 x86_64 서버에서는 안 쓰이지만
# 설치해둬도 무해하므로 공통 requirements에 포함
transformers>=4.57.0
accelerate>=0.34.0
torchaudio>=2.2.0

# LoRA 파인튜닝 (finetune/stt/train_lora.py)
peft>=0.19.0
